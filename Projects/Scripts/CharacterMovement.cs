using Godot;
using System;

public partial class CharacterMovement : Node
{
    /*
    private const float MoveSpeed = 5.0f;

    private Vector3 velocity;

    public override void _PhysicsProcess(float delta)
    {
        // Obtener las entradas del InputMap
        float moveX = Input.GetActionStrength("ui_right") - Input.GetActionStrength("ui_left");
        float moveZ = Input.GetActionStrength("ui_down") - Input.GetActionStrength("ui_up");

        // Calcular la dirección del movimiento
        var direction = new Vector3(moveX, 0, moveZ).Normalized();

        // Aplicar la velocidad al personaje
        velocity = direction * MoveSpeed;

        // Mover el personaje
        //MoveAndSlide(velocity);
    }
    */
}
